spring:
  datasource:
    url:
    username:
    password:
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  security:
    jwt:
      secret:
app:
  jwt:
    secret:
    expiration: 3600000
server:
  port: 8080
logging:
  level:
    com.juliashtal: DEBUG
